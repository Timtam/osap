id = "com.platform.kontakt"
name = "Kontakt"
version = "0.1.0"
engine_api = ">=0.1, <0.2"
entry = "src/main.luau"
license = "MIT"
dependencies = ["com.platform.daw-hosts"]

[capabilities]
require = ["window", "uia", "screen", "input", "speech", "hotkey", "keys", "timer", "log"]
