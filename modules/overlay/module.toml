id = "com.example.overlay"
name = "Overlay Demo"
version = "0.1.0"
engine_api = ">=0.1, <0.2"
entry = "src/main.luau"
license = "MIT"
dependencies = ["com.platform.overlay"]

[capabilities]
require = ["overlay", "speech", "ocr", "input", "hotkey"]
