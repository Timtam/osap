id = "com.example.window-trigger"
name = "Window Trigger Demo"
version = "0.1.0"
engine_api = ">=0.1, <0.2"
entry = "src/main.luau"
license = "MIT"

[capabilities]
require = ["window", "speech", "log"]
